<section class="descripcion">
	<h1><a href="{{ oferta.nombre }}"></a></h1>
	{{ oferta.descripcion }}
	<a href="" class="boton">Comprar</a>
</section>

<section class="galeria">
	<img alt="..." src="{{ asset('uploads/images/' ~ oferta.foto) }}">
	<p class="precio">$ {{ oferta.precio }}  <span> | Descuento {{ oferta.descuento }} %</span></p>
	<p><strong>Condiciones:</strong> {{ oferta.condiciones }}</p>
</section>

<section class="direccion">
	<h2>Disfruta de la oferta en</h2>
	<p>
		<a href="#">{{ oferta.tienda.nombre }}</a> {{ oferta.tienda.direccion }}
	</p>
</section>

<section class="tienda">
	<h2>Sobre la tienda</h2>
	{{ oferta.tienda.descripcion }}
</section>